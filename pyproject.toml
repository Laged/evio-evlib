[tool.uv.workspace]
members = [
    "evio",
    "workspace/libs/*",
    "workspace/plugins/*",
    "workspace/apps/*",
]

[tool.uv]
# Python version requirements are specified in each workspace member's pyproject.toml
